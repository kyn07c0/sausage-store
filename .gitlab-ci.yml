stages:
  - build
  - test

include:
  - template: Security/SAST.gitlab-ci.yml

spotbugs-sast:
  variables:
    COMPILE: "false"
    SAST_JAVA_VERSION: 11
    MAVEN_REPO_PATH: ${CI_PROJECT_DIR}/.m2/repository

build-backend-code-job:
  stage: build  
  only:
    changes:
    - backend/*
  script:
    - echo BUILD_BACKEND_JOB_ID = $CI_JOB_ID >> job.env
    - cd backend
    - mvn package -Dmaven.repo.remote=${CI_PROJECT_DIR}/.m2/repository
  artifacts:
    paths:
      - backend/target/sausage-store-0.0.1-SNAPSHOT.jar
      - ${CI_PROJECT_DIR}/.m2/ 
    reports:
      dotenv: job.env

build-frontend-code-job:
  stage: build  
  only:
    changes:
    - frontend/*
  script:
    - echo BUILD_FRONTEND_JOB_ID = $CI_JOB_ID >> job.env    
    - cd frontend
    - npm install
    - npm run build
  artifacts:
    paths:
      - frontend/dist/frontend
    reports:
      dotenv: job.env

sonarqube-backend-sast:
  stage: test
  image: maven:3.8-openjdk-16
  script:
    - cd backend
    - mvn verify sonar:sonar -Dsonar.qualitygate.wait=true
      -Dsonar.projectKey=${SONAR_PROJECT_KEY}
      -Dsonar.host.url=${SONARQUBE_URL}
      -Dsonar.login=${SONAR_LOGIN}
      -Dsonar.qualitygate.wait=true

#telegram-notification-backend:
#  stage: .post
#  only:
#    changes:
#    - backend/*
#  script:
#    - 'curl -X POST -H "Content-Type: application/json" -d "{\"chat_id\": \"-1001763459775\", \"text\": \"Юрий Крывиня собрал backend. https://gitlab.praktikum-services.ru/api/v4/projects/$CI_PROJECT_ID/jobs/$BUILD_BACKEND_JOB_ID/artifacts?private_token=${SS_TOKEN}\"}" https://api.telegram.org/bot5933756043:AAE8JLL5KIzgrNBeTP5e-1bkbJy4YRoeGjs/sendMessage'
#  needs:
#    - build-backend-code-job

#telegram-notification-frontend:
#  stage: .post
#  only:
#    changes:
#    - frontend/*
#  script:
#    - 'curl -X POST -H "Content-type: application/json" --data "{\"chat_id\": \"-1001763459775\", \"text\": \"Юрий Крывиня собрал frontend. https://gitlab.praktikum-services.ru/api/v4/projects/$CI_PROJECT_ID/jobs/$BUILD_FRONTEND_JOB_ID/artifacts?private_token=${SS_TOKEN}\"}" https://api.telegram.org/bot5933756043:AAE8JLL5KIzgrNBeTP5e-1bkbJy4YRoeGjs/sendMessage'
#  needs:
#    - build-frontend-code-job